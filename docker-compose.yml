services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: bpmn
      POSTGRES_PASSWORD: bpmn_local
      POSTGRES_DB: bpmn_app
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U bpmn -d bpmn_app"]
      interval: 5s
      timeout: 3s
      retries: 5

volumes:
  pgdata:
